{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        { "name": "xelatex", "sidecar": false },
        { "name": "lualatex", "sidecar": false },
        { "name": "pdflatex", "sidecar": false }
      ]
    }
  ]
}
