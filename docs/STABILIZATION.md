# 稳定可用版实施记录

基线：`6d9efbe9348bb78de1dd997c63d1b7fa0344ba03`，来源分支 `arena/01a08903-workspace`。
开发分支：`feat/stable-workspace`。原发布包保留在上一级目录。
逐文件比较并统一换行后，本地发布包源码与远程跟踪源码一致。

## 验收清单

- [x] 克隆并核对基线，建立独立开发分支。
- [x] 统一启动入口：新构建优先，默认固定 localhost:8080；不自动换端口。
- [x] 添加 GitHub Actions 类型检查、测试、构建。
- [x] 保存状态、失败重试、关闭保护与多窗口写入保护。
- [x] 数据库与附件整包备份、校验、原子恢复。
- [x] 项目关联文献/笔记/文档并能直接打开。
- [x] 文献引用插入写作，取消延迟提交，防止切换时遗失最后输入。
- [x] 科研流程与备份恢复回归测试。
- [x] Windows 原生持久化、凭据管理与多文件 TeX 编译。
- [x] Windows 构建、测试与安装包。

## 本地验证（2026-09-11）

- `npm run check`：类型检查通过；18 个测试文件、76 项测试通过；生产构建成功。
- `cargo fmt` 完成；`cargo test --locked -- --include-ignored`：6 项通过，含原生恢复、Windows 凭据写入/读取/删除、真实中文多文件 XeLaTeX + BibTeX 编译。
- 凭据测试在沙箱会话中出现 Windows 1312 错误；在用户会话中复测通过。
- 备份测试包含笔记、文献、PDF 附件、文档、项目关系和布局；恢复到独立 IndexedDB 后重新打开核对；损坏备份被拒绝，密钥未打包。
- 浏览器验证旧数据启动、编辑后立即切换、9/9 插件加载及保存状态。
- 最终页面验证新建项目、关联笔记、点击标题打开原笔记、导出完整备份；项目名称改为页面内输入，避免内嵌浏览器不支持原生 prompt。
- 桌面可执行文件实际启动并响应，原生目录生成 229376 字节 SQLite 文件；`integrity_check=ok`，9 个插件记录已启用，核心迁移 1/2 已应用。本轮未进行桌面窗口的逐项视觉验收。
- `npm run desktop:build` 生成 x64 NSIS 安装包。安装向导及长期使用尚未人工验收。

构建保留现有动态导入/静态导入混用和 chunk 大小提示；不影响本轮构建通过，后续可单独优化加载体积。

备份不包含 API 密钥；恢复到新环境后需重新配置。
真实 AI 服务验收需要用户提供服务商与密钥；连续一周使用需后续实际使用验证。
