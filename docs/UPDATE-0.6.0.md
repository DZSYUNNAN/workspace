# ModuDesk 0.6.0 窗口大小控制器

## 使用方法

1. 从左侧栏打开“窗口大小”。
2. 在模块卡片中拖动滑块，或输入精确数值。
3. 如需实时观察工作台，点击“在工作台悬浮调整”。

## 支持的尺寸

- 左侧、右侧、顶部和底部停靠区域的宽度或高度。
- 同一区域中多个分栏的横向或纵向占比。
- 每个浮动窗口独立的宽度与高度。
- 一键恢复默认区域尺寸、50% 分栏比例和 520 × 400 浮动窗口尺寸。

更改会写入当前工作区已有的布局记录，切换工作区后会读取对应工作区自己的尺寸。插件不要求文件、网络、凭据或 AI 权限。
